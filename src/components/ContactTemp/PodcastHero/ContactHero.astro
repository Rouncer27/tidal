---
import "./contactHero.scss";
import { Image } from "astro:assets";
import heroWave from "../../../assets/white-wave-reverse.png";
const { image, title } = Astro.props.data;
const imageSrc = image?.node?.sourceUrl;
const imageAlt = image?.node?.altText || "";
---

<div class="contact-hero">
  <div class="contact-hero-background">
    <Image
      src={imageSrc}
      alt={imageAlt}
      format="webp"
      widths={[800, 1200, 1600, 2000]}
      sizes="100vw"
      loading="eager"
      inferSize={true}
      class="contact-hero-background-img"
    />
  </div>
  <div class="contact-hero-content">
    <div class="contact-hero-content-container">
      <div>
        <h2 set:html={title} />
      </div>
    </div>
  </div>
  <div class="contact-hero-graphic">
    <img src={heroWave.src} alt={"Mountain background"} />
  </div>
</div>

<script>
  // @ts-nocheck
  import { gsap } from "gsap";
  const image = document.querySelectorAll(".contact-hero-background img");
  const tl = gsap.timeline();
  tl.add("start").from(
    image,
    { filter: "blur(100px)", duration: 2.2 },
    "start",
  );
</script>
